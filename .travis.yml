language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-hubot-gulp' ]; then cd .. && eval "mv $currentfolder generator-hubot-gulp" && cd generator-hubot; fi

